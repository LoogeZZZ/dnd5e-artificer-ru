node_modules/

# LevelDB runtime files (не нужны Foundry, генерируются при работе)
packs/**/LOCK
packs/**/LOG
packs/**/LOG.old

# Build/test scratch (не часть модуля)
.dnd5e-src/
.mech-assembled.json
repro*.mjs
repro*.png

# Промежуточный вывод сопоставления заклинаний
.spell-list.json

# scratch инспекторы/временное
ph-inspect.mjs
.tmp*
